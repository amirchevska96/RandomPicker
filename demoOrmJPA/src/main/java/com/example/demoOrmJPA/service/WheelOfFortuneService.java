//package com.example.demoOrmJPA.service;
//
//import org.springframework.stereotype.Service;
//
//import com.example.demoOrmJPA.domain.WheelOfFortune;
//@Service
//public interface WheelOfFortuneService extends Services<Integer, WheelOfFortune>{
//
////	public void randomPicker(ArrayList<Reward> rg, ArrayList<RewardReceiver> rr) {
////
////		Random rand = new Random();
////		ArrayList<Reward> rewardGiver = rg;
////		ArrayList<RewardReceiver> rewardR = rr;
////		int bound = rewardGiver.size();
////		int randNumb;
////		while (bound > 0) {
////			randNumb = rand.nextInt(bound);
////			System.out.println(rewardGiver.get(randNumb).getRewardGiver() + " " + rewardGiver.get(randNumb).getReward()
////					+ " " + rewardR.get(randNumb));
////			rewardR.remove(randNumb);
////			rewardGiver.remove(randNumb);
////			bound--;
////			Collections.shuffle(rewardGiver);
////
////		}}
//
//	}
//
//
